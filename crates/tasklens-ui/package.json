{
  "name": "@mydoo/tasklens-ui",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@playwright/test": "catalog:",
    "@types/js-yaml": "catalog:",
    "@types/node": "catalog:",
    "js-yaml": "catalog:",
    "wait-port": "catalog:"
  },
  "scripts": {
    "check-format": "prettier --check .",
    "fix-format": "prettier --write .",
    "build": "dx build --platform web",
    "test-e2e": "rm -rf ../../target/dx/tasklens-ui/debug/web/public && dx build --package tasklens-ui --platform web && playwright test"
  }
}
