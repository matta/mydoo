{
  "name": "@mydoo/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "glob": "catalog:"
  },
  "devDependencies": {
    "vitest": "catalog:",
    "tsx": "catalog:",
    "@types/node": "catalog:"
  },
  "scripts": {
    "check-format": "prettier --check .",
    "check-types": "tsc --noEmit",
    "fix-format": "prettier --write .",
    "fix-junit": "tsx fix-junit.ts",
    "test": "vitest run"
  }
}
