{
  "name": "@mydoo/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "glob": "catalog:"
  },
  "devDependencies": {
    "vitest": "catalog:"
  },
  "scripts": {
    "check-dark-mode": "tsx lint-dark-mode.ts",
    "check-format": "prettier --check .",
    "check-types": "tsc --noEmit",
    "fix-format": "prettier --write .",
    "test": "vitest run"
  }
}
