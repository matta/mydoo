This is a dumping ground for context files that we'd like an AI agent
to see but don't want to commit. We check this in so we don't forget
where it is, and so we can test tooling against it. We don't .gitignore files here so agents have access to them without any extra work.
