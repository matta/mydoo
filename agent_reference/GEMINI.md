# Repo-Local Memory

- This project uses `pnpm`. Use `pnpm` for all package management and script execution.
  - `pnpm add <pkg>` instead of `npm install <pkg>`
  - `pnpm add -D <pkg>` instead of `npm install -D <pkg>`
  - `pnpm <script>` instead of `npm run <script>`
