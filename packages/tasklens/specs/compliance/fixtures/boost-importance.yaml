# yaml-language-server: $schema=../schemas/test-case.schema.json
name: Boost Importance
description: >
  Verifies that the `importance` field directly influences task priority,
  ensuring high-importance tasks rank above low-importance ones.
initial_state:
  current_time: '2025-01-01T12:00:00Z'
  tasks:
    - id: Major
      title: High Importance
      importance: 100
    - id: Minor
      title: Low Importance
      importance: 1
steps:
  - name: 1. Verify High Importance > Low Importance
    expected_order:
      - Major
      - Minor
