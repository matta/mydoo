# yaml-language-server: $schema=../schemas/test_case.schema.json
name: Sorting
description: >
  Verifies the fundamental sorting logic: tasks are ranked by Priority
  Descending, then by Outline Order (DFS Pre-order) as a stable tie-breaker.
initial_state:
  current_time: '2025-01-01T12:00:00Z'
  tasks:
    - id: Major
      title: High Priority
      importance: 1000
      desired_credits: 0
    - id: Minor
      title: Low Priority
      importance: 900
      desired_credits: 0
steps:
  - name: 1. Verify Sorting Logic
    expected_order:
      - Major
      - Minor
