{
  // This package uses a hybrid TypeScript configuration:
  // 1. Source files (src/) are exported directly for bundlers (Vite)
  // 2. Declarations (.d.ts) are emitted to dist/ for TypeScript project references
  //
  // This allows:
  // - HMR in consuming apps (they import source directly)
  // - Proper IDE type resolution via project references
  //
  // Build: `tsc --emitDeclarationOnly` generates declarations without JS output
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": ".",
    "jsx": "react-jsx"
  },
  "include": ["src", "tests", "*.config.*", "specs/**/*.json"]
}
